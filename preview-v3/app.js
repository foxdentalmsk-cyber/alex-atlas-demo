(()=>{const panels={idea:{title:'ЗАПУСТИТЬ ИДЕЮ',text:'Разберём, кому и зачем нужен будущий продукт — до того, как начнём тратить деньги на его оформление.',items:['Изучим рынок и конкурентов','Проведём интервью и уточним задачу','Предложим 2–3 стратегии запуска']},repack:{title:'ПЕРЕУПАКОВАТЬ ПРОДУКТ',text:'Найдём, почему ценность продукта считывается слабее, чем она есть на самом деле, и соберём более точную подачу.',items:['Разберём текущую коммуникацию','Определим сильные стороны и провалы','Соберём новую структуру предложения']},site:{title:'СОЗДАТЬ САЙТ',text:'Начнём не с экранов, а со смысла, сценария и роли сайта в продукте — чтобы он работал, а не просто существовал.',items:['Определим задачу и путь пользователя','Соберём структуру и тексты','Запустим рабочую версию']},difference:{title:'НАЙТИ ОТЛИЧИЕ',text:'Поймём, за счёт чего продукт может быть заметнее и убедительнее среди похожих предложений.',items:['Изучим конкурентное поле','Найдём реальные точки отличия','Переведём их в понятную коммуникацию']}};const panel=document.getElementById('taskPanel');document.querySelectorAll('.task-list button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.task-list button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const d=panels[btn.dataset.task];panel.animate([{opacity:.35,transform:'translateY(8px)'},{opacity:1,transform:'translateY(0)'}],{duration:320,easing:'ease-out'});panel.querySelector('h3').textContent=d.title;panel.querySelector('p').textContent=d.text;panel.querySelector('ul').innerHTML=d.items.map(x=>`<li>${x}</li>`).join('')}));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const hero=document.querySelector('.hero'),heroArt=document.querySelector('.hero-art');if(hero&&heroArt){hero.addEventListener('pointermove',e=>{if(innerWidth<760)return;const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;heroArt.style.setProperty('--mouse-x',`${x*-13}px`);heroArt.style.setProperty('--mouse-y',`${y*-8}px`)},{passive:true});hero.addEventListener('pointerleave',()=>{heroArt.style.setProperty('--mouse-x','0px');heroArt.style.setProperty('--mouse-y','0px')})}let ticking=false;const clamp=(n,a,b)=>Math.min(b,Math.max(a,n));const update=()=>{document.querySelectorAll('.parallax-scene').forEach(el=>{const s=parseFloat(el.dataset.scrollSpeed||0),r=el.getBoundingClientRect(),center=r.top+r.height/2-innerHeight/2;el.style.setProperty('--scroll-y',`${center*-s}px`)});document.querySelectorAll('.depth-layer').forEach(el=>{const s=parseFloat(el.dataset.depth||0),host=el.parentElement,r=host.getBoundingClientRect(),center=r.top+r.height/2-innerHeight/2;el.style.setProperty('--layer-y',`${center*-s}px`)});const city=document.getElementById('methodCity');if(city){const r=city.getBoundingClientRect();const progress=clamp((innerHeight-r.top)/(innerHeight+r.height*.65),0,1);const path=city.querySelector('.route-path');if(path)path.style.strokeDashoffset=String(1-progress);if(progress>.08)city.classList.add('route-active');else city.classList.remove('route-active')}ticking=false};addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});addEventListener('resize',update,{passive:true});update();})();