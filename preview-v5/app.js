(()=>{const panels={idea:{title:'ЗАПУСТИТЬ ИДЕЮ',text:'Разберём, кому и зачем нужен будущий продукт — до того, как начнём тратить деньги на его оформление.',items:['Изучим рынок и конкурентов','Проведём интервью и уточним задачу','Предложим 2–3 стратегии запуска']},repack:{title:'ПЕРЕУПАКОВАТЬ ПРОДУКТ',text:'Найдём, почему ценность продукта считывается слабее, чем она есть на самом деле, и соберём более точную подачу.',items:['Разберём текущую коммуникацию','Определим сильные стороны и провалы','Соберём новую структуру предложения']},site:{title:'СОЗДАТЬ САЙТ',text:'Начнём не с экранов, а со смысла, сценария и роли сайта в продукте — чтобы он работал, а не просто существовал.',items:['Определим задачу и путь пользователя','Соберём структуру и тексты','Запустим рабочую версию']},difference:{title:'НАЙТИ ОТЛИЧИЕ',text:'Поймём, за счёт чего продукт может быть заметнее и убедительнее среди похожих предложений.',items:['Изучим конкурентное поле','Найдём реальные точки отличия','Переведём их в понятную коммуникацию']}};const info=document.getElementById('taskInfo');const buttons=[...document.querySelectorAll('.task-list button')];function setTask(key){buttons.forEach(b=>b.classList.toggle('active',b.dataset.task===key));const d=panels[key];info.animate([{opacity:.15,transform:'translateY(8px)'},{opacity:1,transform:'translateY(0)'}],{duration:260,easing:'ease-out'});info.querySelector('h3').textContent=d.title;info.querySelector('p').textContent=d.text;info.querySelector('ul').innerHTML=d.items.map(x=>`<li>${x}</li>`).join('')}buttons.forEach(b=>b.addEventListener('click',()=>setTask(b.dataset.task)));document.querySelectorAll('.quick button').forEach(b=>b.addEventListener('click',()=>{setTask(b.dataset.jump);document.getElementById('method').scrollIntoView({behavior:'smooth',block:'center'})}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12,rootMargin:'0px 0px -8% 0px'});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const path=document.getElementById('routePath');const len=path.getTotalLength();path.style.strokeDasharray=len;path.style.strokeDashoffset=len;let raf=false;function update(){const h=innerHeight;document.querySelectorAll('.depth').forEach(el=>{const s=parseFloat(el.dataset.speed||0);const r=el.getBoundingClientRect();const center=r.top+r.height*.5-h*.5;el.style.transform=`translate3d(0,${center*-s}px,0)`});const method=document.getElementById('method');const mr=method.getBoundingClientRect();const p=Math.max(0,Math.min(1,(h*.82-mr.top)/(mr.height*.72)));path.style.strokeDashoffset=len*(1-p);raf=false}addEventListener('scroll',()=>{if(!raf){requestAnimationFrame(update);raf=true}},{passive:true});addEventListener('resize',update,{passive:true});update();})();